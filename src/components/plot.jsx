import React, { Component } from "react";

class Plot extends Component {
  render() {
    //if the survey is complete, draw the plot. If during a survey, plot info pics etc.
    return <span>This should be a chart</span>;
  }
}

export default Plot;
